# --- Required in production ---
# Staff login master password
ADMIN_PASSWORD=CEC2026

# Python parser service URL (Render free tier) + shared secret
PARSER_SERVICE_URL=http://localhost:8000
BACKEND_SHARED_SECRET=change-me

# Secret for the external cron scheduler hitting /api/cron/cleanup
CRON_SECRET=change-me-cron

# --- Local development only (Vercel deploys authenticate via OIDC) ---
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
