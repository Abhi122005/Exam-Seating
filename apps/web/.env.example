# Master Staff Admin Password (HTTP-only session cookie)
ADMIN_PASSWORD=change-me

# Python PDF Parser Service URL (Render Free Tier or Local Fallback)
PARSER_SERVICE_URL=http://localhost:8000
BACKEND_SHARED_SECRET=change-me

# Vercel Blob Storage Token (Private Read/Write Token)
# Leave empty for zero-config local filesystem fallback (.local_data/)
BLOB_READ_WRITE_TOKEN=

# Cron Automated Cleanup Secret
CRON_SECRET=change-me
